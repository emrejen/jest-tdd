/**
 * 
  Create a calculator class that uses fluent syntax to perform addition and subtraction
  1. The calculator should never throw exceptions
  2. After the first seed additional calls are ignored
  3. Assume all inputs are numbers
  Example:
    new Calculator()
      .seed(10)
      .plus(5)
      .plus(5).
      .minus(2)
      .result() //result = 18
 */

describe.skip('Fluent calculator testing suit', () => {
  it('should work', () => {
    expect(1).toEqual(1);
  });
});
